{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Launch Lsystems (default)",
            "stdio": [
                "tests/failure/missing/semicolon_0.lsy",
                null
            ],
            "preLaunchTask": "Compile",
            "expressions": "native",
            "program": "${workspaceFolder}/build/lsystems"
        }
    ]
}
