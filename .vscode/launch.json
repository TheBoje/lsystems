{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Launch Lsystems (default)",
            "stdio": [
                "examples/default.lsy",
                null
            ],
            "preLaunchTask": "Compile",
            "expressions": "native",
            "program": "${workspaceFolder}/build/lsystems"
        }
    ]
}
